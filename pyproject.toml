[project]
name = "iuu-fishing-radar"
version = "0.1.0"
description = "Open-data pipeline and live map detecting anomalous fishing vessel behavior around Marine Protected Areas"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "gfw-api-python-client",
    "httpx",
    "duckdb",
    "dbt-duckdb",
    "geopandas",
    "shapely",
    "h3",
    "pyod",
    "scikit-learn",
    "fastapi",
    "uvicorn[standard]",
    "redis",
    "prefect",
    "pydantic-settings",
    "pyyaml",
    "prometheus-fastapi-instrumentator",
    "sse-starlette>=3.4.6",
    "slowapi>=0.1.10",
]

[project.optional-dependencies]
dev = [
    "ruff",
    "pytest",
    "pytest-cov",
]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
# B008: FastAPI's Depends()-in-default-argument is the intended idiom, not a bug.
ignore = ["B008"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "httpx>=0.28.1",
]

[tool.hatch.build.targets.wheel]
packages = ["src/iuu_radar"]
