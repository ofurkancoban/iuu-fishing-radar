# Optional k3s deployment

Not built. docker-compose is the source of truth for this project's deployment
(see the root `docker-compose.yml` once Phase 8 lands). k3s manifests mirroring
the compose services will only be added here if a Kubernetes deliverable is
explicitly requested (Phase 11 in `CLAUDE.md`).
